const obj = {
    firstName: 'John',
    lastName: 'Doe',
    age: 30,
    email: 'john.doe@example.com'
};

const numKeys = Object.keys(obj).length;
console.log(`Number of keys/properties in obj: ${numKeys}`); 
